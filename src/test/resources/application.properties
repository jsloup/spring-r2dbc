embedded.postgresql.enabled=true

spring.r2dbc.url=r2dbc:postgresql://${embedded.postgresql.host}:${embedded.postgresql.port}/${embedded.postgresql.schema}
spring.r2dbc.username=${embedded.postgresql.user}
spring.r2dbc.password=${embedded.postgresql.password}
spring.r2dbc.initialization-mode=always